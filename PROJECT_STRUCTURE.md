# 项目结构说明

## 📁 目录结构

```
news_push/
├── 📖 README.md              # 项目文档
├── 🚀 main.py                # 主入口文件
├── 📁 src/                   # 源代码目录
│   ├── 📰 news/             # 新闻推送模块
│   │   ├── news_pusher.py              # 新闻推送器
│   │   ├── global_news_pusher.py       # 全球新闻推送
│   │   ├── get_china_news.py           # 中国新闻获取
│   │   ├── social_media_monitor.py     # 社交媒体监控
│   │   └── smart_pusher.py             # 智能推送器
│   │
│   ├── 📈 stocks/           # 股票监控模块
│   │   ├── multi_stock_monitor.py      # 多股票监控
│   │   ├── hourly_multi_stock_monitor.py # 每小时股票监控
│   │   ├── hourly_alibaba_monitor.py   # 阿里巴巴监控
│   │   ├── auto_stock_notifier.py      # 股票通知器
│   │   └── price_alert_system.py       # 价格警报系统
│   │
│   ├── 🔧 common/           # 公共模块
│   │   ├── news_stock_pusher.py        # 新闻+股票推送器
│   │   ├── auto_push_system.py         # 自动推送系统
│   │   ├── optimized_push_system.py    # 优化推送系统
│   │   └── simple_push_system.py       # 简单推送系统
│   │
│   └── 🛠️  utils/           # 工具模块
│       ├── ai_analyzer.py              # AI分析器
│       ├── monitor_system.py           # 系统监控
│       ├── final_system_check.py       # 系统检查
│       └── check_installation.py       # 安装检查
│
├── 📁 scripts/              # 脚本目录
│   ├── push_manager.sh                 # 推送管理脚本
│   ├── push_control.sh                 # 推送控制脚本
│   ├── auto_monitor.sh                 # 自动监控脚本
│   ├── monitor_1200_push.sh            # 1200推送监控
│   ├── configure_opencode.sh           # OpenCode配置
│   └── install_stock_analysis.sh       # 股票分析安装
│
├── 📁 config/               # 配置文件目录
│   ├── alert_config.json               # 警报配置
│   ├── clawdbot_stock_config.json      # 股票配置
│   ├── social_config.json              # 社交媒体配置
│   └── 📁 data/                        # 数据文件
│       ├── alert_history.json          # 警报历史
│       ├── news_history.json           # 新闻历史
│       └── social_media_history.json   # 社交媒体历史
│
├── 📁 tests/                # 测试目录
│   ├── test_international_news.py      # 国际新闻测试
│   ├── test_detailed_summary.py        # 详细摘要测试
│   ├── test_time_importance.py         # 时间重要性测试
│   ├── test_news_links.py              # 新闻链接测试
│   ├── simple_deduplication_test.py    # 去重测试
│   └── test_deduplication.py           # 去重功能测试
│
├── 📁 logs/                 # 日志目录
│   ├── auto_push.log                   # 自动推送日志
│   └── simple_push.log                 # 简单推送日志
│
└── 📄 cron_setup.sh         # 定时任务配置脚本
```

## 🚀 使用方法

### 1. 运行主程序
```bash
python main.py
```

### 2. 使用管理脚本
```bash
# 查看系统状态
./scripts/push_manager.sh status

# 测试推送
./scripts/push_manager.sh test

# 查看日志
./scripts/push_manager.sh log

# 设置定时任务
./scripts/push_manager.sh setup
```

### 3. 直接运行模块
```bash
# 运行新闻推送
python -m src.common.auto_push_system --run

# 运行股票监控
python -m src.stocks.multi_stock_monitor

# 运行系统测试
python -m tests.test_international_news
```

## ⚙️ 配置说明

### 定时任务配置
系统需要配置两个定时任务：
1. **主系统**: 每小时整点运行 `src/common/auto_push_system.py`
2. **备份系统**: 每小时整点运行 `src/common/simple_push_system.py`

运行配置脚本：
```bash
./cron_setup.sh
```

### 配置文件
- `config/alert_config.json`: 价格警报配置
- `config/clawdbot_stock_config.json`: 股票监控配置
- `config/social_config.json`: 社交媒体配置

## 🔧 开发说明

### 添加新功能
1. **新闻功能**: 添加到 `src/news/` 目录
2. **股票功能**: 添加到 `src/stocks/` 目录
3. **工具功能**: 添加到 `src/utils/` 目录
4. **公共功能**: 添加到 `src/common/` 目录

### 运行测试
```bash
# 运行所有测试
python -m pytest tests/

# 运行特定测试
python -m tests.test_international_news
```

## 📊 系统特点

1. **模块化设计**: 新闻和股票功能分离
2. **易于扩展**: 清晰的目录结构
3. **完整测试**: 包含测试套件
4. **详细日志**: 独立的日志目录
5. **配置管理**: 统一的配置目录

## 🐛 故障排除

### 常见问题
1. **导入错误**: 确保 `src/` 目录在Python路径中
2. **权限问题**: 确保脚本有执行权限 `chmod +x scripts/*.sh`
3. **路径问题**: 使用相对路径时确保在项目根目录

### 日志查看
```bash
# 查看自动推送日志
tail -f logs/auto_push.log

# 查看简单推送日志
tail -f logs/simple_push.log
```

## 📝 更新记录

### 2026-02-04: 项目结构重构
- 将新闻和股票功能分离到独立目录
- 创建清晰的模块化结构
- 更新所有文件路径引用
- 添加完整的项目文档